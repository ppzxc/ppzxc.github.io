{
  "private": true,
  "name": "site",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "gatsby build && gh-pages -d public -b gh-pages",
	"d": "gh-pages -d public -b gh-pages"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "gatsby": "^2.13.32",
    "gatsby-theme-simple-blog": "*",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
